import { useState, useEffect } from 'react'
import { ethers } from "ethers"
import { Row, Col, Card } from 'react-bootstrap'
import {
  Link
} from "react-router-dom";

export default function HTTP404({ marketplace, account }) {
    return (
        <main style={{ padding: "1rem 0" }}>
        <h2>404</h2>
      </main>
    );
}